<Window x:Class="Auth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Auth"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Авторизация" FontSize="40" HorizontalAlignment="Center"/>
        <StackPanel Grid.Row="1" MaxWidth="500" Margin="0, 20">
            <TextBlock Text="Логин" FontSize="20"/>
            <TextBox Name="LoginTB"/>

            <TextBlock Text="Пароль" FontSize="20"/>
            <TextBox Name="PasswordTB"/>

            <Button Content="Login" FontSize="20" Margin="0, 30" Click="Button_Click"/>
        </StackPanel>

    </Grid>
</Window>
